import { RequiredInputsDirective } from './required-inputs.directive';

describe('RequiredInputsDirective', () => {
  it('should create an instance', () => {
    const directive = new RequiredInputsDirective();
    expect(directive).toBeTruthy();
  });
});

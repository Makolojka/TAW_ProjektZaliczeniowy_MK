import { StepsFormatPipe } from './steps-format.pipe';

describe('StepsFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new StepsFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
